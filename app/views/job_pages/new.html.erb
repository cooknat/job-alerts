<h2>Add new Job Pages here</h2>

 <%= form_for @jobPage do |f| %>
   <%= f.label :name %>
   <%= f.text_field :name %>
 
   <%= f.label :url %>
   <%= f.text_field :url %>
 
   <%= f.submit "Add Page" %>
 <% end %>
